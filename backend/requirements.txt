# Backend requirements for VideoRAG — Hybrid Architecture
# ========================================================

# --- Web Framework ---
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# --- Database ---
sqlalchemy>=2.0.31
psycopg2-binary>=2.9.9
alembic>=1.13.0

# --- Authentication ---
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# --- Step 1: Indexing ---
openai-whisper>=20231117            # ASR transcription
ultralytics>=8.2.0                  # YOLOv8 visual tagger
opencv-python>=4.10.0               # Video frame sampling
scenedetect[opencv]>=0.6.4          # Scene detection
ffmpeg-python>=0.2.0                # Audio/video extraction
easyocr>=1.7.0                      # OCR for slide/whiteboard text extraction

# --- Step 2: Retrieval ---
sentence-transformers>=3.0.0        # Text embeddings
qdrant-client>=1.9.0                # Qdrant Vector search
langchain>=0.2.0                    # LLM chains for timestamp resolution
langchain-community>=0.2.0          # Ollama + LlamaCpp integrations

# --- Step 3: Multimodal LLM ---
ollama>=0.4.0                       # Ollama Python client (local VLM, no API key)
openai>=1.35.0                      # GPT-4o (optional, cloud fallback)
# google-generativeai>=0.7.0        # Gemini (optional, cloud fallback)

# --- Utilities ---
python-dotenv>=1.0.1
pydantic>=2.8.0
pydantic-settings>=2.3.0
email-validator>=2.0.0
httpx>=0.27.0
numpy>=1.26.0
Pillow>=10.4.0
